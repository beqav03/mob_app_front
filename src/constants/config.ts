// Global configuration constants
export const MOCK_DELAY = 800; // Simulated API delay in milliseconds

// Gemini API Configuration
// NOTE: In a real production app, never store API keys directly in the code.
// Use a proxy server or React Native Config variables.
export const GEMINI_API_KEY = 'YOUR_GEMINI_API_KEY_HERE';
export const GEMINI_MODEL = 'gemini-pro';

// App Constants
export const CURRENCY_SYMBOL = '$';
export const TAX_RATE = 0.1; // 10%
